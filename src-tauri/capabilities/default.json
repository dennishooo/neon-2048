{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities — the game runs fully offline and needs no privileged APIs.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
